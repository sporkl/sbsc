# Spread of Beliefs in (Partly) Modularized Communities

system dependencies:
- igraph

generate build folder with `cmake -B build -DCMAKE_BUILD_TYPE=Release`.
then build using `cmake --build build --config Release`.
